# skill 回写

产物相对上次构建被直接修改过。你的任务：把产物中元指令未覆盖的有价值内容
回写进元指令，使下次重建不丢失这些修改。

## 要求

- 对照元指令与产物 `skills/<name>/SKILL.md`，识别新增或变更的实质内容
  （回写只由 SKILL.md 的变更触发；如目录下 `assets/`、`references/` 也有
  直接修改，一并纳入对照）
- 回写进元指令正文；元指令是意图层，用意图与约束的语言表述，
  不是照抄 SKILL.md 文本
- 保持元指令 frontmatter 不变
- 只修改元指令这一个文件；不改产物，不提交
- 正文以中文为主；术语、type 名、命令、代码与标识保留英文
