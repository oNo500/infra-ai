# [项目名称]

[1-2 句话描述这个 monorepo 的整体用途]

## 包结构

| 包/应用 | 路径 | 职责 |
|---------|------|------|
| [name] | `[packages/name]` | [一句话说明] |
| [name] | `[apps/name]` | [一句话说明] |

## 统一命令

```bash
# 安装所有依赖
[install command]

# 构建所有包
[build command]

# 运行所有测试
[test command]
```

## 单独启动

```bash
# 启动 [包/应用名]
[command]
```

> 详细规则见 `.claude/rules/`。各包/应用有独立的 `CLAUDE.md` 和 `.claude/rules/`。
